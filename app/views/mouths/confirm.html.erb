<h3>以下の内容で、投稿する。</h3>
<%= form_with(model: @mouth, local: true) do |form| %>
  <%= form.hidden_field :content %>
  <p>内容:<%= @mouth.content %></p>
  <%= form.submit  "投稿する" %>
  <%= form.submit  "戻る", name: 'back' %>
<% end %>
